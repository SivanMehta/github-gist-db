/**
 * Takes a leveldb database, compresses it all into minified string
 * @param {LevelDB} datastore
 * @param {Function} cb
 */
exports.deflate = function(datastore) {
  cb("Database Snapshot")
}

/**
 * Takes the given datastore and replaces its contents
 * with the string
 * @param {LevelDB} datastore
 * @param {String} str
 * @param {Function} cb
 */
exports.inflate = function(datastore, str, cb) {
  cb({ "Go" : "Knicks" })
}
